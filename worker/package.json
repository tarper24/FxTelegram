{
  "name": "fxtelegram-worker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^6.0.0",
    "vitest": "^2.0.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@cf-wasm/photon": "^0.3.4"
  }
}
